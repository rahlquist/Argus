# Template Skill

Copy this folder to start a new skill:

```bash
cp -r skills/_template skills/<your-skill-name>
```

Then edit `SKILL.md` — replace every `<...>` placeholder and follow the section
order below. Keep logic in `scripts/`, docs in `references/`.

See `../CONTRIBUTING.md` and `../skills/README.md` (catalog).
